import 'package:flutter/material.dart';

/// Color scheme
const _primaryColor = Color(0xff03045e);
const _accentColor = Color(0xffF72585);

/// General widget colors
const unselectedTabColor = Colors.white38;
const selectedTabColor = _accentColor;

const bottomAppBarColor = _primaryColor;

const qrBorderColor = _accentColor;

const darkModeBaseColor = Color(0xff121212);

/// Tab colors
final historyTabBackgroundColor =
    Color.alphaBlend(_primaryColor.withOpacity(0.08), darkModeBaseColor);
